# TECH DEBT — технический долг проекта calc_model

Приоритеты: 🔴 высокая | 🟡 средняя | 🟢 низкая.
После закрытия пункта переносить его в раздел «Закрыто» с датой.

---

## Открытые задачи

### Безопасность
- [ ] 🔴 **Отозвать токен `ghp_eBcp...7EeN`** — он был передан в открытом виде в переписке. Выпустить fine-grained токен с минимальными правами (только `calc_model`, contents:write) и обновить `/root/.git-credentials` + `~/.bashrc` + `.env`.
- [ ] 🟡 Рассмотреть переход с `credential.helper store` (открытый текст на диске) на `cache` или CLI-github аутентификацию.

### Репозиторий / CI
- [ ] 🟡 Нет `.gitignore` полного покрытия (сейчас только `.env`) — добавить `__pycache__/`, `*.pyc`, `.venv/`, временные xlsx.
- [ ] 🟡 Нет описания проекта в README (только заголовок `# calc_model`) — добавить: назначение `cal_v2.py`, структура, как запускать.
- [ ] 🟢 Настроить CI (GitHub Actions): lint + smoke-тест `cal_v2.py`.

### Код (cal_v2.py) — предварительный осмотр
- [ ] 🟡 Провести ревью `cal_v2.py`: покрытие тестами отсутствует (нет файлов `test_*`).
- [ ] 🟡 Синхронизировать логику кода с `Metrology_Core_EURACHEM_v2.xlsx` — проверить, что формулы EURACHEM в коде соответствуют таблице.
- [ ] 🟢 Требования зависимостей не зафиксированы (нет `requirements.txt` / `pyproject.toml`).

---

## Реестр технического долга WLS v3 (заполнен по рекомендации пользователя, 2026-09-26)

Формат: ID | Категория | Описание | Причина | Риск | Митигация.

### TD-01 · Математика / WLS · Сингулярность при x=0 для весов 1/x и 1/x²
- **Описание:** В весах используется `IF(x>0, 1/x^k, NA())`; нулевой уровень (холостая проба) получает вес #N/A.
- **Причина:** Физический смысл весов 1/x^k требует строгого x>0; u(y)/y → ∞ при x→0 из-за шума прибора s0. Холостая проба присутствует почти всегда.
- **Риск:** При выборе весов 2/3 наличие x=0 ломает всю регрессию (все коэффициенты #N/A); механическое исключение x=0 искажает оценку сдвига b0.
- **Митигация:**
  - Вариант А: модифицированные веса 1/(x+ε)² или 1/(a+b·x)² (как в chemCal/LIMS); в Excel — 1/(x+LOD)².
  - Вариант Б: исключать x=0 из WLS, но обязательно использовать OLS для b0 и зоны около нуля.
  - Компромисс для Excel (реализовать): жёсткий FAIL на «Валидации», если MIN(x)=0 и выбраны веса 2/3 + предупреждение на «Вводе».

### TD-02 · Метрология / WLS · Некорректное масштабирование ковариационной матрицы
- **Описание:** Ячейки B125–B127 умножают на остаточную дисперсию WLS (B124 = Σwᵢeᵢ²/(n−2)), что корректно только при идеальной априорной модели дисперсии u(y)∝x.
- **Причина:** Упрощение: веса задаются априори (1/x²), истинная σ²(x)=σ₀²·x² неизвестна.
- **Риск:** Если модель дисперсии неверна, ДИ для x_pred математически некорректны (занижены/завышены).
- **Митигация:**
  - Вариант А: проверка адекватности модели дисперсии — анализ стандартизованных остатков WLS (eᵢ·√wᵢ) на гомоскедастичность.
  - Вариант Б: итеративный WLS (IWLS) для уточнения весов по данным.

### TD-03 · Метрология / EURACHEM · Нет теста Lack-of-Fit (LoF)
- **Описание:** Повторности (столбцы C,D,E) есть, но SS_res не разделяется на SS_pure_error и SS_lack_of_fit.
- **Причина:** Код считает s(yᵢ), но не вычисляет F-статистику LoF.
- **Риск:** По EURACHEM (п. E.4.5) без LoF нельзя доказать адекватность линейной модели; нелинейность может маскироваться весами.
- **Митигация:** Добавить на «Валидацию»: dfPE=Σ(nᵢ−1), dfLoF=n_levels−2, F=(SSLoF/dfLoF)/(SSPE/dfPE), сравнение с F_крит(α=0.05).

### TD-04 · Метрология / k-фактор · Жёстко задан k=2
- **Описание:** Расширенная неопределённость всегда U=k·u_c с k=2; ν_eff для WLS не учитывается.
- **Причина:** Упрощение формул для Excel.
- **Риск:** При n=5–6 и низких концентрациях истинный t-фактор 2.45–3.18 → занижение U на 20–30%.
- **Митигация:** Формула Уэлча–Саттертуэйта для ν_eff (для WLS — с поправкой на веса); k=T.INV.2T(0.05, ν_eff) вместо константы 2.

### TD-05 · Архитектура Excel · Хрупкость hardcoded-диапазонов (11:110, 9:108)
- **Описание:** SUMPRODUCT/COUNTIFS ссылаются на «запас» строк.
- **Причина:** Стремление оставить место под данные пользователя.
- **Риск:** Вставка/удаление строки или сортировка ломают формулы или дают скрытые ошибки.
- **Митигация:** Умные таблицы (openpyxl `worksheet.table.Table`, structured references вида `CalibrationData[[Концентрация x]]`) либо динамические диапазоны OFFSET+COUNTA.

### TD-06 · Валидация / Код · Нет численного и стохастического контроля (MC)
- **Описание:** Реализован только аналитический метод; MC на 100k итераций в Excel без VBA/Python невозможен.
- **Причина:** Ограничение среды Excel.
- **Риск:** Ошибку в знаке/скобке в сложных корреляционных членах обратного предсказания WLS невозможно отследить «глазом».
- **Митигация:** Python-скрипт-валидатор: читает xlsx, прогоняет данные через scipy.stats и statsmodels.WLS, пишет PASS/FAIL на лист «Валидация».

### Тройная валидация (обязательное требование, из разбора TD-01/TD-03/TD-06)
1. **Аналитическая кросс-валидация** (Python vs Excel): расхождение коэффициентов WLS ≤ 1e-8; иначе — ошибка знаков в формулах (напр. cov_w).
2. **Численная валидация**: конечные разности/возмущение y_obs на ±δ для проверки корреляционного члена 2·(x0−x̄_w)·cov(b0,b1) в u(x_pred).
3. **Эмпирическая валидация (покрытие на CRM)**: попадание x_CRM в [x_pred±U] ≥95%; если интервал уже — B124 занижает дисперсию, нужен поправочный коэффициент.

### Рекомендации по структуре cal_v3.py (к исполнению)
- Заменить жёсткие диапазоны на ListObjects/structured references.
- Вынести расчёт ŷ, e, e² с листа «Ввод» на «Регрессию»/«Диагностику остатков» (разделить ввод и расчёт).
- Data validation + условное форматирование: блокировать веса 2/3 при MIN(x)==0.

## Архитектурное ревью cal_v4.py (2026-09-26, сохранено из чата)

Ревью фиксирует: ядро метрологических расчётов готово для лабораторной эксплуатации
(после независимой валидации). Закрытые блоки: ОМНК, WLS, исключение x=0,
ковариационная матрица, Welch–Satterthwaite, Lack-of-Fit, профиль U(x), диагностика,
автовалидация, цветовая индикация. Дальше — долги второго уровня.

### 2. Технические долги (TD-07…TD-12)
- [ ] 🔴 **TD-07** Жёстко прошитые диапазоны `9:108` / `100` / `200` — скрытый предел модели (≤100 точек). → динамические диапазоны (Excel Table/ListObject, именованные диапазоны, INDEX()).
- [ ] 🔴 **TD-08** Дублирование формулы u²(x_pred): «Неопределенность» B2, «Профиль U(x)» C, «Неопределенность по точкам» K — 3 копии одного алгоритма. → вынести в единый `ModelVariance(x)`.
- [ ] 🟡 **TD-09** Магические числа (50, 100, 199, 1E-12, 0.5, 2) → конфигурация `CONFIG` (`MAX_POINTS`, `STD_DF`, `D_LIMIT_LOW/HIGH`, `EPS`).
- [ ] 🟡 **TD-10** Формулы собираются длинными f-строками → мини-DSL / `FormulaBuilder()` / шаблоны.
- [ ] 🟡 **TD-11** Нет централизованной библиотеки оформления (заливки/границы/ширины повторяются) → `theme.py`.
- [ ] 🔴 **TD-12** Нет контроля версии книги (версия, дата генерации, версия Python, git commit) — критично для ISO 17025.

### 3. Архитектурные долги (AD-01…AD-06)
- [ ] 🔴 **AD-01** Excel одновременно интерфейс и вычислитель. → разделение: Excel=UI, Python Engine=расчёт, Excel Report=отчёт.
- [ ] 🟡 **AD-02** Нет единого графа зависимостей (модель как DAG вместо ссылок ячеек) → автопостроение бюджета.
- [ ] 🟡 **AD-03** Нет объектной модели измерения (`Standard`, `Instrument`, `Solution`, `Calibration`, `Measurement`, `Budget`).
- [ ] 🟡 **AD-04** Пользовательские модели отсутствуют (Y=(m×P×C)/V).
- [ ] 🟡 **AD-05** Нет символьного дифференцирования (SymPy → ∂Y/∂x вместо ручных производных).
- [ ] 🟡 **AD-06** Нет общего ковариационного движка (универсальная Σ, cᵀΣc для любых моделей).

### 4. Методологические долги (MD-01…MD-04)
- [ ] 🟡 **MD-01** Нет библиотеки распределений (нормальное, прямоугольное, треугольное, трапеция, логнормальное, t, пользовательское).
- [ ] 🟡 **MD-02** Нет корреляций между приборами (одна пипетка несколько раз → матрица корреляций).
- [ ] 🟡 **MD-03** Нет серийных разбавлений (дерево, каждый узел — собственный бюджет).
- [ ] 🟡 **MD-04** Нет выбора метода обратной калибровки (классическая, ISO 11095, inverse prediction, weighted inverse).

### 5. Долги валидации (VD-01…VD-03)
- [ ] 🟡 **VD-01** Нет Golden Dataset (NIST / EURACHEM / JCGM examples, автопрогон каждого примера).
- [ ] 🟡 **VD-02** Нет Monte-Carlo сравнения (GUM vs MC vs Finite Difference, таблица допусков).
- [ ] 🟡 **VD-03** Нет round-trip валидации (Формула→Excel→Python→SymPy→MC→сравнение).

### 6. Дорожная карта
- **v4.1 (рефактор):** TD-07…TD-12.
- **v4.5 (ядро модели):** AD-02/03, MD-01/02/03, библиотека приборов, матрица корреляций.
- **v5.0 (GUM Engine):** SymPy→чувствительность→ковариации→Welch→MC→PDF; Excel=оболочка.
- **v6.0 (Audit Edition):** журнал изменений, цифровые подписи, история сертификатов, прослеживаемость, отчёт ISO/IEC 17025.

### Главный архитектурный вывод
Проект просится на трёхслойную архитектуру **GUI ↔ вычислительное ядро ↔ аудитор**:
Excel перестаёт быть единственным источником истины, Python — эталонный исполнитель
по JCGM 100/101, аудитор — независимая проверка (round-trip, конечные разности, MC,
прослеживаемость). Это важнее добавления ещё десятка формул: новые модели измерений
будут подключаться как модули, а не копии формул по листам.

## Закрыто
| Дата | Пункт |
|------|-------|
| 2026-09-26 | **TD-02** Закрыт: полная ковариационная форма (XᵀWX)⁻¹ + D = φ_w/s²_ОМНК, проверка 0.5<D<2 (PASS) |
| 2026-09-26 | **TD-03** Закрыт: LoF-тест (SS_PE, SS_LoF, F, F_crit, FINV) для ОМНК и WLS на листе «Диагностика» (PASS) |
| 2026-09-26 | **TD-04** Закрыт: k по Стьюденту (TINV) с ν_eff по Уэлчу–Саттертуэйту (PASS, k=2.008 при ν=50.7) |
| 2026-09-26 | **TD-06** Закрыт: `validate_v4.py` (numpy+scipy) — совпадение с Excel до 1e-5 во всех режимах |
| 2026-09-25 | Инициализация git, привязка к внешнему репо, первый push в main |
| 2026-09-25 | Настроена бесшовная аутентификация (credential store), проверено тестовым push |
